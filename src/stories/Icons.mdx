import { Meta } from '@storybook/blocks';
import icon from '../../build/icons/icon.svg';

<Meta title="Design System/Icons" />

# Icons

The 7K icon set. Currently the primary brand mark is the app icon.

## Icon library

<div
  style={{
    display: 'flex',
    gap: '24px',
    flexWrap: 'wrap',
    marginTop: '24px',
  }}
>
  <div
    style={{
      border: '2px solid var(--border-default)',
      padding: '24px',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: '12px',
      background: 'var(--bg-base)',
      minWidth: '160px',
    }}
  >
    <img src={icon} alt="7K app icon" style={{ width: '64px', height: '64px' }} />
    <span
      style={{ fontFamily: 'var(--font-mono)', fontSize: '12px', color: 'var(--text-secondary)' }}
    >
      icon
    </span>
    <span
      style={{ fontFamily: 'var(--font-mono)', fontSize: '11px', color: 'var(--text-tertiary)' }}
    >
      build/icons/icon.svg
    </span>
  </div>
</div>

## Usage

```tsx
import icon from '7k-design-system/build/icons/icon.svg';

<img src={icon} alt="7K" width="32" height="32" />;
```

Add new SVG files to `build/icons/` and they will be copied to `dist/build/icons/` on build.
